<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- MainActivity extends AppCompatActivity (required for BiometricPrompt),
         so the theme must descend from Theme.AppCompat. DayNight lets the
         system bars follow the device theme; Compose owns everything else. -->
    <style name="Theme.FamilyMoney" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.FamilyMoney.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.FamilyMoney</item>
    </style>
</resources>
