# דוח QA — הכסף שלנו

תאריך: 6 בספטמבר 2026
מכשיר בדיקה: אמולטור `Medium_Phone_API_35` (Android 15, API 35)
גרסה: 1.0.0 · `com.familymoney`

---

## סיכום

| בדיקה | תוצאה |
|---|---|
| קומפילציה (Kotlin + KSP + Room) | ✅ עובר |
| בדיקות יחידה | ✅ 71/71 |
| בדיקות מכשיר (instrumented) | ✅ 9/9 |
| התקנה על מכשיר | ✅ הצליחה |
| הפעלה ללא קריסה | ✅ 0 קריסות |
| ניווט בין כל הטאבים | ✅ עובד |
| AI מחובר ומחזיר תשובה מבוססת נתונים | ✅ עובד |
| RTL, עברית, פונט Heebo | ✅ תקין |

---

## בדיקות יחידה — 71 עוברות

| קובץ | בדיקות | מה נבדק |
|---|---:|---|
| `RecommendationEngineTest` | 29 | כל כללי ההמלצה, חישוב תזרים, כרית ביטחון, זיהוי חריגות, יעדים, מיון לפי עדיפות |
| `CompoundTest` | 15 | ריבית דריבית מול נוסחאות סגורות, תחזית לילדים, הפקדה חודשית נדרשת |
| `CsvImporterTest` | 14 | פורמטים ישראליים, חובה/זכות, מפרידים, קידודים, פרסור סכומים |
| `RecurringDetectorTest` | 13 | זיהוי הוצאות קבועות, מנויים, מקרי קצה שלא אמורים להיתפס |

### דוגמאות למה שנבדק

```
✓ יתרה צפויה = 9,800 + 4,000 − 2,400 − (200×10) = ₪9,400
✓ סכום פנוי = 9,400 − 3,000 (כרית) = ₪6,400
✓ סכום פנוי לעולם לא שלילי
✓ חריגה מזוהה רק מעל 125% מהממוצע — לא ב־110%
✓ נזילות נמוכה ⇒ המערכת ממליצה להעביר אחוז קטן יותר
✓ ריבית דריבית תואמת את נוסחת האנונה עד 0.5 ש"ח על 10 שנים
✓ הפקדה חודשית נדרשת, כשמופעלת, מגיעה בדיוק ליעד
✓ 3 חיובים באותו חודש אינם הוראת קבע
```

---

## בדיקות מכשיר — 9 עוברות

רצו על האמולטור מול Room אמיתי ו־Compose אמיתי:

```
✓ roomSchemaCreatesAndAcceptsAProfile
✓ demoSeedPopulatesEveryTable              (>100 עסקאות, 3 חשבונות, 3 כרטיסים, 6 תקציבים…)
✓ snapshotAndEngineProduceRealOutputForSeededData
✓ categoryLearningRemembersTheUsersChoice
✓ recurringDetectionRunsAgainstSeededHistory
✓ themeComposesWithHebrewTextAndBundledFont
✓ darkThemeAlsoComposes
✓ settingsStoreEncryptsAndReadsBack
✓ qrCodeGeneratesForAnInviteLink
```

---

## בדיקה ידנית על המכשיר

| מסך | נבדק | תוצאה |
|---|---|---|
| Onboarding | 6 שלבים, ולידציה, טעינת דמו | ✅ |
| בית | כרטיס הירו, המלצה חכמה, קרוסלת המלצות, ניווט תחתון, FAB | ✅ |
| עסקאות | חיפוש, 3 שכבות סינון, קיבוץ לפי חודש, סיווג אוטומטי עם אימוג'י | ✅ |
| יעדים | פרוגרס, נדרש־לחודש, חסר־ליעד, כפתור הפקדה | ✅ |
| השקעות | שווי, תשואה, פיזור לפי סוג, דיבידנדים, החזקות | ✅ |
| פרופיל | ספירת שותפים/חשבונות/כרטיסים, תפריטים | ✅ |
| AI | שאלה אמיתית → תשובה מבוססת נתונים | ✅ |

### אימות תשובת ה־AI

לשאלה **"כמה אני יכול להוציא החודש?"** התקבל:

> היתרה הצפויה עד סוף החודש היא ‎־₪4,337, וכאשר מוסיפים את כרית הביטחון של
> ₪3,000 מתקבל חיסור של ‎־₪7,337…

**הצלבה מול המנוע:** ‎־4,337 זהה בדיוק לתחזית התזרים שהמנוע חישב, ו־3,000 הוא
בדיוק ערך כרית הביטחון בפרופיל. כלומר ה־AI עונה מהנתונים ולא ממציא.

---

## באגים שנמצאו ותוקנו

| # | באג | סיבה | תיקון |
|---|---|---|---|
| 1 | קריסה בהפעלה: `Could not load font` | קבצי Heebo שהורדו מ־GitHub לא היו TTF תקינים (magic `0a0a0a0a`) | הורדה מחדש מ־`fonts.gstatic.com` דרך Google Fonts API + אימות magic bytes. בנוסף `FontLoadingStrategy.OptionalLocal` כך שפונט פגום יורד לפונט מערכת במקום לקרוס |
| 2 | קריסה צפויה: theme לא־AppCompat | `MainActivity` יורש `AppCompatActivity` (נדרש ל־BiometricPrompt) אך ה־theme היה Material | שינוי ל־`Theme.AppCompat.DayNight.NoActionBar` |
| 3 | 30 שגיאות קומפילציה | `Pill` ו־`SectionHeader` הגדירו `onClick` לפני פרמטרים אחרים, כך ש־trailing lambda נקשר לפרמטר הלא נכון | סידור מחדש — הלמבדה תמיד אחרונה |
| 4 | `Unable to delete directory` בבנייה | OneDrive נועל קבצים בתיקיית `build` | הפניית פלט הבנייה ל־`~/AndroidBuilds/FamilyMoney` |
| 5 | ה־AI הציג `**כוכביות**` | המודל מחזיר Markdown; Compose מרנדר טקסט גולמי | ניקוי Markdown ב־`GroqClient` + הנחיה מפורשת בפרומפט |

---

## מגבלות ידועות

1. **חיבור בנקים אוטומטי לא פעיל.** דורש רישיון שירות מידע פיננסי מבנק ישראל.
   הארכיטקטורה (`BankProvider`) מוכנה; פעיל היום: ייבוא CSV והזנה ידנית.

2. **תשלום NFC לא ממומש.** דורש טוקניזציה, הסמכת EMV והסכם מנפיק. האפליקציה
   מזהה יכולות NFC/HCE ומפנה ל־Google Wallet. תואם §16 במפרט (זה V2).

3. **ביצועי אמולטור.** באמולטור עם GPU תוכנתי כל המערכת נכנסה ל־ANR (גם
   Launcher ו־SystemUI). מעבר ל־`-gpu host` פתר לגמרי. על מכשיר אמיתי לא רלוונטי.

4. **סנכרון משפחתי לא נבדק בין שני מכשירים.** הקוד והחוקים כתובים ו־Firebase
   מחובר, אך אימות מלא דורש שני מכשירים.

---

## איך להריץ מחדש

```bash
gradlew.bat testDebugUnitTest
```

```bash
gradlew.bat connectedDebugAndroidTest
```

```bash
gradlew.bat assembleDebug
```
